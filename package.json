{
  "name": "exfu-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "check": "astro check",
    "test": "playwright test",
    "format": "prettier --write src tests scripts *.mjs *.ts *.json",
    "format:check": "prettier --check src tests scripts *.mjs *.ts *.json",
    "test:visual": "node scripts/visual-review.mjs",
    "social-card": "node scripts/create-social-card.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.2",
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.58.2",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.9.3"
  }
}
